function json_response = test_connect(headers, config)
    c = clock;
    response.message = sprintf('Matlab: %s connected!', headers.Content.app_name);
    response.time = sprintf('%d
	json_response = mat2json(response);